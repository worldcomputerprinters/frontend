# Copy to .env if you need to point the frontend at a deployed backend.
# Local dev works without this file — api.js already defaults to localhost.
VITE_API_URL=https://world-computer-backend.onrender.com
